# Domain configuration
FRONTEND_DOMAIN=nucla.mathalama.dev
BACKEND_DOMAIN=auth-nucla.mathalama.dev
VITE_API_URL=https://auth-nucla.mathalama.dev

# Database configuration
# Note: MaxConns is hardcoded in backend/cmd/server/main.go (currently 15)
# For deployments with multiple backends, reduce MaxConns accordingly to avoid exceeding PostgreSQL max_connections limit
POSTGRES_USER=nucla_prod
POSTGRES_PASSWORD=your_secure_password_here
POSTGRES_DB=nucla_prod

# Backend configuration
PORT=8080
JWT_SECRET=generate_a_very_long_random_string_here
ALLOWED_ORIGINS=https://nucla.mathalama.dev,http://localhost:5173,http://localhost:8080,http://localhost
FRONTEND_URL=https://nucla.mathalama.dev

# Google OAuth
GOOGLE_CLIENT_ID=your_google_client_id.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=your_google_client_secret

# SMTP Email Configuration (Optional, for notifications)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=465
SMTP_USER=your_email@gmail.com
SMTP_PASS=your_app_password
SMTP_FROM=your_email@gmail.com

# Web Push
VAPID_PUBLIC_KEY=your_vapid_public_key_here
VAPID_PRIVATE_KEY=your_vapid_private_key_here
